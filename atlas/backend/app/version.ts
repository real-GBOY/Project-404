/**
 * Identity of the running application. The deployable is **Atlas RE OS** — a
 * real-estate operating system — built on AURIC Core. It is not "Core" and
 * does not share Core's version number, and it is a fully separate deployment
 * from Mizan (own database, own users, own JWT secret).
 */
export const APP_NAME = "Atlas RE OS";
export const APP_CODENAME = "atlas";
export const APP_VERSION = "0.1.0";
