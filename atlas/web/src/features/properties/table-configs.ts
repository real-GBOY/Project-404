import type { TableConfigRegistry } from "@/features/shared/table-types";

/** Table configs for: projects, buildings, availability, pricing. */
export const propertiesTableConfigs: TableConfigRegistry = {};
