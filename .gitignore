node_modules/
dist/
build/
coverage/
*.log
*.tsbuildinfo

# env
.env
.env.local
.env.*.local

# runtime data
/storage/files/
/var/

# editor / OS
.DS_Store
.idea/
*.swp

# secrets / deploy keys — never commit
/me
*.pem
id_rsa
id_ed25519

deploy.env

# MSW browser worker — regenerated by older msw installs; the in-browser mock is gone (tests use msw/node)
mizan/web/public/mockServiceWorker.js
